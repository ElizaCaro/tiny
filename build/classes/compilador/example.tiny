int myfunc(int a,boolean b)
begin
	write x;
	return (a+b);
end;

boolean factorial()
begin
	read x;
	int fact,b,c,f,r,g[8];
	fact:=0;
	if 0 < x then
		fact := 1;
	  	repeat
	    	fact := fact * x;
	    	x := x - 1;
	  	until x = 0;
	end;
	return x;
end;

void myproc()
begin
	int a[10];
	for(x:=1;x<10;x:=x+1)
		a[8]:=x;
		if x>=7 and x<10 then
			write jodete;
		end;
	end;
end;

begin 
	x:= myproc(p,e);

	b:= a+factorial();
	

end;